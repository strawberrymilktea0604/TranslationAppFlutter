// Speech feature uses SpeechTranslationData from the data source layer
// and SpeechTranslationEntity from the domain layer.
// No separate model is needed since the data source directly parses
// the backend JSON into SpeechTranslationData (same pattern as OCR).
