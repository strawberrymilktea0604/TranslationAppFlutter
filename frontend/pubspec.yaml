name: frontend
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+3

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State Management — Bloc/Cubit pattern (copilot-instructions §3.1)
  flutter_bloc: ^9.1.0
  bloc: ^9.0.0

  # State Management — Riverpod (for Vocabulary feature)
  flutter_riverpod: ^2.4.0
  riverpod: ^2.4.0

  # HTTP client
  http: ^1.1.0

  # Database — SQLite (for local vocabulary storage)
  sqflite: ^2.3.0
  path: ^1.8.3

  # Value equality for State/Entity classes
  equatable: ^2.0.7

  # Functional programming — Either<Failure, T> pattern (copilot-instructions §3.2)
  dartz: ^0.10.1

  # Dependency Injection container (copilot-instructions §2.2)
  get_it: ^8.0.3

  # Routing — declarative navigation with deep linking
  go_router: ^14.8.1

  # Secure token storage — JWT must NOT be in SharedPreferences (copilot-instructions §3.5)
  flutter_secure_storage: ^9.2.4

  # JSON serialization annotations
  json_annotation: ^4.9.0

  # Isar Database (community fork — compatible with Dart 3.10+)
  isar_community: ^3.3.2
  isar_community_flutter_libs: ^3.3.2
  path_provider: ^2.1.2

  # Fonts
  google_fonts: ^6.2.1
  flutter_test:
    sdk: flutter
  flutter_gen: 5.9.0
  shimmer: ^3.0.0
  flutter_tts: ^4.2.5
  speech_to_text: ^7.0.0
  image_picker: ^1.2.2
  cached_network_image: ^3.4.1
  flutter_image_compress: ^2.4.0

  # Image cropping — allows users to select text region before OCR (UC06).
  # Uses UCrop on Android and TOCropViewController on iOS.
  image_cropper: ^12.2.1

  # Audio recording — records from microphone to file (UC05 Speech-to-Text).
  # Uses native AudioRecord/MediaCodec (Android), AVFoundation (iOS).
  record: ^6.2.0

  # WebSocket — realtime sync notifications from server (UC09).
  # RFC 6455 / Protocol 13. Used by RealtimeSyncService.
  web_socket_channel: ^3.0.1

dev_dependencies:
  mockito: ^5.4.4
  bloc_test: ^10.0.0

  # JSON serialization code generator
  json_serializable: ^6.8.0

  # Isar Code Generator (community fork)
  isar_community_generator: ^3.3.2

  # Code generation runner
  build_runner: any
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/
    - assets/branding/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # see https://flutter.dev/to/font-from-package

dependency_overrides:
  connectivity_plus: ^5.0.2
  path_provider_foundation: 2.5.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/app_icon.png
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: assets/branding/app_icon.png
  min_sdk_android: 23
  remove_alpha_ios: true
  web:
    generate: true
    image_path: assets/branding/app_icon.png
    background_color: "#FFFFFF"
    theme_color: "#FFFFFF"
  windows:
    generate: true
    image_path: assets/branding/app_icon.png
    icon_size: 48
  macos:
    generate: true
    image_path: assets/branding/app_icon.png

flutter_native_splash:
  color: "#FFFFFF"
  color_dark: "#111827"
  image: assets/branding/app_icon.png
  image_dark: assets/branding/app_icon.png
  android: true
  ios: true
  web: true
  android_12:
    color: "#FFFFFF"
    color_dark: "#111827"
    image: assets/branding/app_icon.png
    image_dark: assets/branding/app_icon.png
    icon_background_color: "#FFFFFF"
    icon_background_color_dark: "#111827"

